
<div class="container mt-5">
  <% @lists.each do |list| %>
    <h2>
      <%= link_to list.name, list_path(list), class: 'links' %>
    </h2>
  <% end %>
    <%= link_to "Create", new_list_path, class: 'btn btn-primary' %>
</div>
